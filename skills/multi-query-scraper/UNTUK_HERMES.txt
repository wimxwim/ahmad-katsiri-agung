================================================================================
UNTUK HERMES: CARA PAKAI MULTI-QUERY SCRAPER SKILL
================================================================================

RINGKAS BANGET:

1️⃣  TEST CEPAT (ALWAYS WORKS):
    cd ~/.agents/skills/multi-query-scraper
    python3 -u test.py
    ✅ Output: ~/Desktop/Test_Multi_Query_Output.xlsx

2️⃣  REAL SEARCH (PERLU CHROME):
    Step 1: Start Chrome (SEKALI DOANG):
      ~/.agents/skills/multi-query-scraper/setup_chrome.sh

    Step 2: Run scraper:
      cd ~/.agents/skills/multi-query-scraper
      python3 -u coba_kuliner.py
      ✅ Output: ~/Desktop/Kuliner_Jaksel.xlsx

================================================================================

DETAIL GITU KALAU HERMES INGIN TAHU:

Skill ini buat search bisnis manapun di lokasi manapun:
  - Input: Lokasi (Jakarta Selatan), Keyword (Kuliner, PT, Restoran)
  - Output: Excel dengan 3 sheet (All, With Website, Without Website)
  - Cara: Multi-query + aggressive scroll + fuzzy dedup

Files:
  - test.py → PT mock (cepat, gak perlu Chrome)
  - coba_kuliner.py → Kuliner real (perlu Chrome)
  - setup_chrome.sh → Setup Chrome otomatis
  - HERMES_INSTRUCTIONS.md → Detail lengkap

================================================================================

KALAU ERROR GIMANA?

❌ "Chrome gak running di localhost:9222"
   → Jalanin setup_chrome.sh dulu

❌ "0 hasil dari Maps"
   → Coba lagi, biasanya 2x jalan

❌ "browser-act gagal connect"
   → Chrome mungkin mati, restart dengan setup_chrome.sh

================================================================================

YANG PENTING HERMES TAHU:

✅ Script SUDAH SIAP, gak perlu write code lagi
✅ Tinggal CALL skill, bukan build skill
✅ Mock test ALWAYS WORKS (test.py)
✅ Real test WORKS tapi butuh Chrome running dulu

❌ Jangan mikir branching/error handling kompleks
❌ Jangan coba optimize browser automation
❌ Jangan multi-process scraper

Just run. Itu aja. Simple.

================================================================================

QUICK START UNTUK HERMES:

# Test (no Chrome needed):
python3 -u ~/.agents/skills/multi-query-scraper/test.py

# Real (Chrome needed):
~/.agents/skills/multi-query-scraper/setup_chrome.sh
python3 -u ~/.agents/skills/multi-query-scraper/coba_kuliner.py

# Check output:
ls -lh ~/Desktop/*.xlsx

Done. Selesai. Jangan overthinking.

================================================================================
